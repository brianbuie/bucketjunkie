Frontend
	prototype front-end UI/UX strategy
	reevaluate foundation, find better scss/frontend framework
	style reset password email
  "No way associated with the NBA" footer notice
  slowly move methods to API calls on the front-end

Backend

  bugs
    logout sometimes doesn't remove account info from menu bar
    saving account info replaces image if none is selected
    flashes sometimes get lost
    flashes on form errors and res.redirect('back') makes form submit disabled
    roster times are weird, make sure node and mongo time is nsync
    properly send back req.body on form failures (login, register, create league)

  features
    add chat to activity feed
    user flair
      allow mods to check "show moderator flair" when sending message
    sort public leagues by time until deadline
    add number of players to league listing
    add description to league listing
    account notifications

  league
    only allow creator to edit league
    set deadline (edit?)
    delete league (add deleted flag to schema)

  draft
    snake order
    Require a draftlist
    deadline for when autodraft will perform
    user will be skipped if draft list runs out
    notify user if current computed autodraft will leave them without players
    switch league open to false

  rosters
    replace player on roster
    propose trade system
  
  player performance prediction
    tomorrow's top predictions
    next 7 days top prediction
    league average

  schedule tasks
    perform autodraft
    check for player images via nba.com script
    update players and team info (switch nba service to just API calls?)
    get boxscores
    score rosters
    update player performance prediction based on league rules

Deployment
  setup subdomain & digitalocean server
  stage environment