Frontend
	prototype front-end UI/UX strategy
	reevaluate foundation, find better scss/frontend framework
	style reset password email
  "No way associated with the NBA" footer notice

Backend

  bugs
    logout sometimes doesn't remove account info from menu bar
    saving account info replaces image if none is selected
    get actual league updates when updating league

  features
    return 404 on leagues that are private and not a member of
    add "joinToken" to schema, populated by shortId
      /join/:joinToken allows user to join
      add ref query to login/registration flow to redirect after completion
    remove "open" from league creation
      default true, switch false when draft starts
    add chat to activity feed
    user flair
      allow mods to check "show moderator flair" when sending message

  league rules
    point values for each stat type
    unique rosters
    number of roster spots
    unique by position?
      might not have enough data for this
    time limit for draft
    roster size

  draft
    this could get hairy with performance issues.
    autodraft based on list
    skip after time limit
    once all players have had a chance to draft all spots, free-for-all

  rosters
    effective date (default tomorrow)
      get rosters
        scoring -> most recent roster with effective date less than gamedate
        current -> most recent roster
    propose trade system
  
  player performance prediction
    tomorrow's top predictions
    next 7 days top prediction
    league average

  schedule tasks
    check for player images via nba.com script
    update players and team info
    get boxscores
    score rosters
    update player performance prediction based on league rules

Deployment
  setup subdomain & digitalocean server
  stage environment