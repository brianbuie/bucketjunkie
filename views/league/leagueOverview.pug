extends ../layout

include _leagueForm

block content
  p
    strong= league.name
  p= league.description
  p
    a(href=`/league/${league._id}/edit`) Edit 
    a(href=`/league/${league._id}/join`) Join
  p 
    strong Members
  pre
    != helpers.dump(league)