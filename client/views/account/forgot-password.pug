extends ../layout

block content
  .row
    .col-sm-6.col-lg-3.mx-auto.p-3.bg-light
      h2.text-center Password Reset
      form(action="/account/forgot-password" method="POST")
        .form-group
          label(for="email") Email
          input#email.form-control(type="email" name="email")
        .py-3
          button.btn.btn-block.btn-success(type="submit") Send Reset →
