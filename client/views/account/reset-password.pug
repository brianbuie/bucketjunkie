extends ../layout

block content
  .row
    .col-sm-6.col-lg-3.mx-auto.p-3.bg-light
      h2.text-center New Password
      form(method="POST")
        .form-group
          label(for="password") Password
          input#password.form-control(type="password" name="password")
        .form-group
          label(for="confirm-password") Again
          input#confirm-password.form-control(type="password" name="confirm-password")
        .py-3
          button.btn.btn-block.btn-success(type="submit") Reset Password →